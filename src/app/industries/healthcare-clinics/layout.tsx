import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "Healthcare Clinics Generator Solutions | PowerHold Generators",
  description: "Backup power for clinics — critical loads, after-hours installs, and emergency generator service.",
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
