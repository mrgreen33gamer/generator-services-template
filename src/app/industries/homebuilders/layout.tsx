import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "Homebuilders Generator Solutions | PowerHold Generators",
  description: "Generator packages for Central Texas homebuilders — standby installs, transfer switches, and production scheduling.",
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
