import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "Agriculture & Ranches Generator Solutions | PowerHold Generators",
  description: "Generator install and service for ranches, wells, barns, and rural properties across Central Texas.",
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
